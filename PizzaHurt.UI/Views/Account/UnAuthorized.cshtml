<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Unauthorized Access</title>
    <style>
        /* Global Styles */
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            font-family: 'Quicksand', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: floralwhite; /* Light red background */
        }

        /* Container for the Unauthorized Message */
        .unauthorized-container {
            text-align: center;
            background-color: #ffffff; /* White background for contrast */
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Soft shadow */
            max-width: 400px;
            width: 80%;
        }

        /* Unauthorized Title */
        .unauthorized-container h1 {
            font-size: 2.5rem;
            color: #721c24; /* Darker red for text */
            margin-bottom: 10px;
        }

        /* Message Paragraph */
        .unauthorized-container p {
            font-size: 1.1rem;
            color: #495057; /* Slightly dark grey */
            margin-bottom: 20px;
        }

        /* Home Button Styles */
        .home-button {
            display: inline-block;
            background-color: #007bff; /* Blue button */
            color: black; /* White text */
            font-weight:900;
            text-decoration: none;
            padding: 12px 20px;
            border-radius: 5px;
            transition: background-color 0.3s easein;
        }

        /* Hover Effect for Home Button */
        .home-button:hover {
            background-color: #0056b3; /* Darker blue on hover */
            color:black;
        }

        /* Key Animation Style */
        .key {
            width: 60px;
            height: 60px;
            margin: 0 auto 20px;
            background-color: #721c24; /* Same color as the heading */
            border-radius: 50%;
            position: relative;
            animation: spin 2s infinite linear; /* Infinite spinning animation */
        }

        /* Key Bit Style */
        .key:before {
            content: "";
            position: absolute;
            width: 15px;
            height: 15px;
            background-color: #ffffff; /* Key head */
            top: 18px;
            left: 8px;
            border-radius: 2px;
        }

        /* Animation Definition */
        @@keyframes spin {
            0% {
                transform: rotate(0deg);
            }
            100% {
                transform: rotate(360deg);
            }
        }
    </style>
</head>
<body>

<div class="unauthorized-container">
    <div class="key"></div> <!-- Animated Key Icon -->
    <h1>403 - Unauthorized</h1>
    <p>You do not have permission to view this page.</p>
    <a href="/" class="home-button">Go to Home Page</a> <!-- Navigation Button -->
</div>

</body>
</html>
